#!/usr/bin/env bash
# scripts/check-rls.sh — stub for rep 1 (no Supabase). Exists for consistency
# with the 10 non-negotiable scripts. Reps with Supabase must replace this
# with a real check that every table has an RLS policy referencing tenant_id.
set -euo pipefail
echo "[check-rls] rep 1 has no Supabase — stub passing."
exit 0
